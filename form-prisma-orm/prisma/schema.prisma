// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"                 
  // A linha 'url' foi removida devido ao erro P1012
}

model User {
  id    Int     @id @default(autoincrement())
  name  String
  email String  @unique 
}